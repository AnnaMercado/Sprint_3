<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Update Task</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gray-100">
<?php if (isset($taskToUpdate)): ?>
    

    <!-- Navbar -->
    <nav class="bg-blue-600 p-4 shadow-md">
        <div class="container mx-auto">
            <h2 class="text-white text-xl font-bold"><i class="fas fa-edit"></i> Update Task</h2>
        </div>
    </nav>

    <div class="container mx-auto my-10 p-6 bg-white shadow-lg rounded-lg w-2/3">
        
            <h1 class="text-2xl font-bold mb-6">Update Task</h1>
            
            <form method="POST" action="">
            <input type="hidden" name="id" value="<?= htmlspecialchars($taskToUpdate['id']) ?>">
            

                <!-- Name Field -->
                <div class="mb-4 flex items-center">
                    <label for="name" class="w-1/4 text-gray-700 font-semibold">Name:</label>
                    <input type="text" id="name" name="name" 
                        value="<?= isset($taskToUpdate['name']) ? htmlspecialchars($taskToUpdate['name']) : ''; ?>"
                        class="w-3/4 p-2 border border-gray-300 rounded-lg focus:outline-none focus:ring-2 focus:ring-blue-500" required>
                </div>

                <!-- Task Field -->
                <div class="mb-4 flex items-center">
                    <label for="task" class="w-1/4 text-gray-700 font-semibold">Task:</label>
                    <textarea id="task" name="task" rows="3"
                        class="w-3/4 p-2 border border-gray-300 rounded-lg focus:outline-none focus:ring-2 focus:ring-blue-500" required><?= isset($taskToUpdate['task']) ? htmlspecialchars($taskToUpdate['task']) : ''; ?></textarea>
                </div>

                <!-- Created At Field -->
                <div class="mb-4 flex items-center">
                    <label for="created_at" class="w-1/4 text-gray-700 font-semibold">Created At:</label>
                    <input type="datetime-local" id="created_at" name="created_at" 
                        value="<?= isset($taskToUpdate['created_at']) ? date('Y-m-d\TH:i', strtotime($taskToUpdate['created_at'])) : ''; ?>"
                        class="w-3/4 p-2 border border-gray-300 rounded-lg focus:outline-none focus:ring-2 focus:ring-blue-500">
                </div>

                <!-- Status Dropdown -->
                <div class="mb-4 flex items-center">
                    <label for="status" class="w-1/4 text-gray-700 font-semibold">Status:</label>
                    <select name="status" id="status" required 
                        class="w-3/4 p-2 border border-gray-300 rounded-lg focus:outline-none focus:ring-2 focus:ring-blue-500">
                        <option value="pending" <?= (isset($taskToUpdate['status']) && $taskToUpdate['status'] == 'pending') ? 'selected' : ''; ?>>Pending</option>
                        <option value="completed" <?= (isset($taskToUpdate['status']) && $taskToUpdate['status'] == 'completed') ? 'selected' : ''; ?>>Completed</option>
                        <option value="in_progress" <?= (isset($taskToUpdate['status']) && $taskToUpdate['status'] == 'in_progress') ? 'selected' : ''; ?>>In Progress</option>
                    </select>
                </div>

                <!-- Buttons -->
                <div class="flex justify-between mt-6">
                    <button type="submit" class="bg-green-500 text-white px-6 py-2 rounded-lg hover:bg-green-600 transition w-1/2 mr-2">
                        <i class="fas fa-check"></i> Update Task
                    </button>
                    <a href="/tasks" class="bg-red-500 text-white px-6 py-2 rounded-lg hover:bg-red-600 transition w-1/2 ml-2 text-center">
                        <i class="fas fa-times"></i> Cancel
                    </a>
                </div>
            
<?php endif; ?>
            </form>
    </div>

</body>
</html>
